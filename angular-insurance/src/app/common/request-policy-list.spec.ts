import { RequestPolicyList } from './request-policy-list';

describe('RequestPolicyList', () => {
  it('should create an instance', () => {
    expect(new RequestPolicyList()).toBeTruthy();
  });
});
